import React, { useEffect, useState } from 'react'

const GenreGrid:React.FC = () => {
  const [genres, setGenrs] = useState<string[]>([]);
  const [error, setError] = useState<string>("");

  useEffect(() => {
    

  }, [])

  return (
    <div>
      
    </div>
  )
}

export default GenreGrid
